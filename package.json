{
  "name": "medatlas",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "demo:seed": "node scripts/seed-demo.mjs",
    "format": "prettier . --write",
    "lint": "eslint .",
    "typecheck": "pnpm -r typecheck",
    "test:smoke": "node --test --experimental-strip-types tests/*.test.ts"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "prettier": "^3.2.5",
    "typescript": "^5.6.0"
  }
}
